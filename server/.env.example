# Configuración de la Base de Datos
DB_HOST=localhost
DB_USER=root
DB_PASS=tu_password_mysql
DB_NAME=sistema_meseros

# Configuración del Servidor
PORT=3000

# Para producción, usa una base de datos real como:
# - PlanetScale (MySQL gratis)
# - Railway PostgreSQL (gratis)
# - MongoDB Atlas (gratis)